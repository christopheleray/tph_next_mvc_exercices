<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= item.original_price %></td>
    <td><%= item.discount_percentage %></td>
    <td><%= item.price %></td>
    <td><%= link_to 'Details', item_path(item.id) %></td>
  </tr>
<% end %>